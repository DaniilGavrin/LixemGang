name: Deploy Flutter Web

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4

    - name: 🐦 Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.35.5'
        channel: 'stable'

    - name: 🌐 Build Web
      run: flutter build web --release

    - name: 🔑 Setup SSH (for cleanup & reload)
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.DEPLOY_HOST }}
        username: ${{ secrets.DEPLOY_USER }}
        password: ${{ secrets.DEPLOY_PASSWORD }}
        script: |
          # Очистить СОДЕРЖИМОЕ папки, но не саму папку
          rm -rf /var/www/lixemgang/*
          # Или, если есть скрытые файлы (редко для Flutter):
          # rm -rf /var/www/lixemgang/{*,.*}
          # Но осторожно с .* — можно удалить .well-known!

    - name: 🚀 Upload files
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.DEPLOY_HOST }}
        username: ${{ secrets.DEPLOY_USER }}
        password: ${{ secrets.DEPLOY_PASSWORD }}
        source: "build/web/*"
        target: "/var/www/lixemgang/"
        # rm: false ← убираем!

    - name: ✅ Reload nginx
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.DEPLOY_HOST }}
        username: ${{ secrets.DEPLOY_USER }}
        password: ${{ secrets.DEPLOY_PASSWORD }}
        script: sudo systemctl reload nginx